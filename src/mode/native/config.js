/**
 * The native mode config
 */

export const config = {
  //
  scrollContent: {
    padding: false
  }
};
/**
 * validate the options
 * @export
 * @param {any} ops
 */
export function validator() {
  let renderError = false;
  return renderError;
}
